# Peer packages (@deepseek-ai/*) are provided by the DSH host at runtime.
# Without this, a standalone pnpm install tries to fetch them from the
# registry and fails when the scope is not published there.
auto-install-peers=false
strict-peer-dependencies=false
