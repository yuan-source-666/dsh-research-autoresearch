# Peer packages (@deepseek-ai/*) are provided by the DSH host at runtime.
# pnpm: skip auto-installing host peers on standalone checkouts.
# npm (used for the nested prepare install of git deps): legacy resolution so
# peerDependencies are NOT fetched from the registry.
auto-install-peers=false
strict-peer-dependencies=false
legacy-peer-deps=true
